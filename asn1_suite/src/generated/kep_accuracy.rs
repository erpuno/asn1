// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct KEPAccuracy {
    pub seconds: Option<ASN1Integer>,
    pub millis: Option<ASN1Integer>,
    pub micros: Option<ASN1Integer>,
}

impl KEPAccuracy {
    pub fn new(seconds: Option<ASN1Integer>, millis: Option<ASN1Integer>, micros: Option<ASN1Integer>) -> Self {
        Self {
            seconds,
            millis,
            micros,
        }
    }
}

impl DERParseable for KEPAccuracy {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let seconds = if let Some(node) = nodes.peek().map(|n| n.clone()) { match ASN1Integer::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let millis = if let Some(node) = nodes.peek().map(|n| n.clone()) { match ASN1Integer::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let micros = if let Some(node) = nodes.peek().map(|n| n.clone()) { match ASN1Integer::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {
                seconds: seconds,
                millis: millis,
                micros: micros,
            })

        })
    }
}

impl DERSerializable for KEPAccuracy {
    fn serialize(&self, serializer: &mut Serializer) -> Result<(), ASN1Error> {
        serializer.write_sequence(|serializer| {
            if let Some(val) = &self.seconds { val.serialize(serializer)?; }
            if let Some(val) = &self.millis { val.serialize(serializer)?; }
            if let Some(val) = &self.micros { val.serialize(serializer)?; }
            Ok(())
        })
    }
}
