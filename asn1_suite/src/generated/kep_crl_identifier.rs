// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct KEPCrlIdentifier {
    pub crlissuer: Name,
    pub crl_issued_time: UTCTime,
    pub crl_number: Option<ASN1Integer>,
}

impl KEPCrlIdentifier {
    pub fn new(crlissuer: Name, crl_issued_time: UTCTime, crl_number: Option<ASN1Integer>) -> Self {
        Self {
            crlissuer,
            crl_issued_time,
            crl_number,
        }
    }
}

impl DERParseable for KEPCrlIdentifier {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let crlissuer = Name::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let crl_issued_time = UTCTime::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let crl_number = if let Some(node) = nodes.peek().map(|n| n.clone()) { match ASN1Integer::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {
                crlissuer: crlissuer,
                crl_issued_time: crl_issued_time,
                crl_number: crl_number,
            })

        })
    }
}

impl DERSerializable for KEPCrlIdentifier {
    fn serialize(&self, serializer: &mut Serializer) -> Result<(), ASN1Error> {
        serializer.write_sequence(|serializer| {
            self.crlissuer.serialize(serializer)?;
            self.crl_issued_time.serialize(serializer)?;
            if let Some(val) = &self.crl_number { val.serialize(serializer)?; }
            Ok(())
        })
    }
}
