// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct CertificateExtensionsAuthorityKeyIdentifier {
    pub key_identifier: Option<KeyIdentifier>,
    pub authority_cert_issuer: Option<GeneralNames>,
    pub authority_cert_serial_number: Option<CertificateSerialNumber>,
}

impl CertificateExtensionsAuthorityKeyIdentifier {
    pub fn new(key_identifier: Option<KeyIdentifier>, authority_cert_issuer: Option<GeneralNames>, authority_cert_serial_number: Option<CertificateSerialNumber>) -> Self {
        Self {
            key_identifier,
            authority_cert_issuer,
            authority_cert_serial_number,
        }
    }
}

impl DERParseable for CertificateExtensionsAuthorityKeyIdentifier {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let key_identifier = if let Some(node) = nodes.peek().map(|n| n.clone()) { match KeyIdentifier::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let authority_cert_issuer = if let Some(node) = nodes.peek().map(|n| n.clone()) { match GeneralNames::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let authority_cert_serial_number = if let Some(node) = nodes.peek().map(|n| n.clone()) { match CertificateSerialNumber::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {
                key_identifier: key_identifier,
                authority_cert_issuer: authority_cert_issuer,
                authority_cert_serial_number: authority_cert_serial_number,
            })

        })
    }
}

impl DERSerializable for CertificateExtensionsAuthorityKeyIdentifier {
    fn serialize(&self, serializer: &mut Serializer) -> Result<(), ASN1Error> {
        serializer.write_sequence(|serializer| {
            if let Some(val) = &self.key_identifier { val.serialize(serializer)?; }
            if let Some(val) = &self.authority_cert_issuer { val.serialize(serializer)?; }
            if let Some(val) = &self.authority_cert_serial_number { val.serialize(serializer)?; }
            Ok(())
        })
    }
}
