// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct LDAPMessage {
    pub message_id: MessageID,
    pub protocol_op: LDAPMessageProtocolOpChoice,
    pub controls: Option<Controls>,
}

impl LDAPMessage {
    pub fn new(message_id: MessageID, protocol_op: LDAPMessageProtocolOpChoice, controls: Option<Controls>) -> Self {
        Self {
            message_id,
            protocol_op,
            controls,
        }
    }
}

impl DERParseable for LDAPMessage {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let message_id = MessageID::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let protocol_op = LDAPMessageProtocolOpChoice::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let controls = if let Some(node) = nodes.peek().map(|n| n.clone()) { match Controls::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {
                message_id: message_id,
                protocol_op: protocol_op,
                controls: controls,
            })

        })
    }
}

impl DERSerializable for LDAPMessage {
    fn serialize(&self, serializer: &mut Serializer) -> Result<(), ASN1Error> {
        serializer.write_sequence(|serializer| {
            self.message_id.serialize(serializer)?;
            self.protocol_op.serialize(serializer)?;
            if let Some(val) = &self.controls { val.serialize(serializer)?; }
            Ok(())
        })
    }
}
