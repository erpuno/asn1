// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct LDAPControl {
    pub control_type: LDAPOID,
    pub criticality: ASN1Boolean,
    pub control_value: Option<ASN1OctetString>,
}

impl LDAPControl {
    pub fn new(control_type: LDAPOID, criticality: ASN1Boolean, control_value: Option<ASN1OctetString>) -> Self {
        Self {
            control_type,
            criticality,
            control_value,
        }
    }
}

impl DERParseable for LDAPControl {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let control_type = LDAPOID::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let criticality = ASN1Boolean::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let control_value = if let Some(node) = nodes.peek().map(|n| n.clone()) { match ASN1OctetString::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {
                control_type: control_type,
                criticality: criticality,
                control_value: control_value,
            })

        })
    }
}

impl DERSerializable for LDAPControl {
    fn serialize(&self, serializer: &mut Serializer) -> Result<(), ASN1Error> {
        serializer.write_sequence(|serializer| {
            self.control_type.serialize(serializer)?;
            self.criticality.serialize(serializer)?;
            if let Some(val) = &self.control_value { val.serialize(serializer)?; }
            Ok(())
        })
    }
}
