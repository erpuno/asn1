// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct InformationFrameworkChopSpecification {
    pub specific_exclusions: Option<Vec<ASN1Node>>,
    pub minimum: BaseDistance,
    pub maximum: Option<BaseDistance>,
}

impl InformationFrameworkChopSpecification {
    pub fn new(specific_exclusions: Option<Vec<ASN1Node>>, minimum: BaseDistance, maximum: Option<BaseDistance>) -> Self {
        Self {
            specific_exclusions,
            minimum,
            maximum,
        }
    }
}

impl DERParseable for InformationFrameworkChopSpecification {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let specific_exclusions = if let Some(node) = nodes.peek().map(|n| n.clone()) {
                          if let rust_asn1::asn1::Content::Constructed(collection) = &node.content {
                               nodes.next();
                               Some(collection.clone().into_iter().collect())
                          } else {
                               None
                          }
                       } else { None };
            let minimum = BaseDistance::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let maximum = if let Some(node) = nodes.peek().map(|n| n.clone()) { match BaseDistance::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {
                specific_exclusions: specific_exclusions,
                minimum: minimum,
                maximum: maximum,
            })

        })
    }
}

impl DERSerializable for InformationFrameworkChopSpecification {
    fn serialize(&self, serializer: &mut Serializer) -> Result<(), ASN1Error> {
        serializer.write_sequence(|serializer| {
            if let Some(val) = &self.specific_exclusions { val.serialize(serializer)?; }
            self.minimum.serialize(serializer)?;
            if let Some(val) = &self.maximum { val.serialize(serializer)?; }
            Ok(())
        })
    }
}
