// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct KEPOtherHashAlgAndValue {
    pub hash_algorithm: AlgorithmIdentifier,
    pub hash_val: OtherHashValue,
}

impl KEPOtherHashAlgAndValue {
    pub fn new(hash_algorithm: AlgorithmIdentifier, hash_val: OtherHashValue) -> Self {
        Self {
            hash_algorithm,
            hash_val,
        }
    }
}

impl DERParseable for KEPOtherHashAlgAndValue {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let hash_algorithm = AlgorithmIdentifier::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let hash_val = OtherHashValue::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            Ok(Self {
                hash_algorithm: hash_algorithm,
                hash_val: hash_val,
            })

        })
    }
}

impl DERSerializable for KEPOtherHashAlgAndValue {
    fn serialize(&self, serializer: &mut Serializer) -> Result<(), ASN1Error> {
        serializer.write_sequence(|serializer| {
            self.hash_algorithm.serialize(serializer)?;
            self.hash_val.serialize(serializer)?;
            Ok(())
        })
    }
}
