// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct BasicAccessControlUserPermission {
    pub precedence: Option<Precedence>,
    pub protected_items: ProtectedItems,
    pub grants_and_denials: GrantsAndDenials,
}

impl BasicAccessControlUserPermission {
    pub fn new(precedence: Option<Precedence>, protected_items: ProtectedItems, grants_and_denials: GrantsAndDenials) -> Self {
        Self {
            precedence,
            protected_items,
            grants_and_denials,
        }
    }
}

impl DERParseable for BasicAccessControlUserPermission {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let precedence = if let Some(node) = nodes.peek().map(|n| n.clone()) { match Precedence::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let protected_items = ProtectedItems::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let grants_and_denials = GrantsAndDenials::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            Ok(Self {
                precedence: precedence,
                protected_items: protected_items,
                grants_and_denials: grants_and_denials,
            })

        })
    }
}

impl DERSerializable for BasicAccessControlUserPermission {
    fn serialize(&self, serializer: &mut Serializer) -> Result<(), ASN1Error> {
        serializer.write_sequence(|serializer| {
            if let Some(val) = &self.precedence { val.serialize(serializer)?; }
            self.protected_items.serialize(serializer)?;
            self.grants_and_denials.serialize(serializer)?;
            Ok(())
        })
    }
}
