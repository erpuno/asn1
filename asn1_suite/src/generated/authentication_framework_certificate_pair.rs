// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct AuthenticationFrameworkCertificatePair {
    pub issued_by_this_ca: Option<Certificate>,
    pub issued_to_this_ca: Option<Certificate>,
}

impl AuthenticationFrameworkCertificatePair {
    pub fn new(issued_by_this_ca: Option<Certificate>, issued_to_this_ca: Option<Certificate>) -> Self {
        Self {
            issued_by_this_ca,
            issued_to_this_ca,
        }
    }
}

impl DERParseable for AuthenticationFrameworkCertificatePair {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let issued_by_this_ca = if let Some(node) = nodes.peek().map(|n| n.clone()) { match Certificate::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let issued_to_this_ca = if let Some(node) = nodes.peek().map(|n| n.clone()) { match Certificate::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {
                issued_by_this_ca: issued_by_this_ca,
                issued_to_this_ca: issued_to_this_ca,
            })

        })
    }
}

impl DERSerializable for AuthenticationFrameworkCertificatePair {
    fn serialize(&self, serializer: &mut Serializer) -> Result<(), ASN1Error> {
        serializer.write_sequence(|serializer| {
            if let Some(val) = &self.issued_by_this_ca { val.serialize(serializer)?; }
            if let Some(val) = &self.issued_to_this_ca { val.serialize(serializer)?; }
            Ok(())
        })
    }
}
