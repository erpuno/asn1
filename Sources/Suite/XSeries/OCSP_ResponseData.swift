// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023—2024 Namdak Tönpa.
import SwiftASN1
import Foundation

@usableFromInline struct OCSP_ResponseData: DERImplicitlyTaggable, Hashable, Sendable {
    @inlinable static var defaultIdentifier: ASN1Identifier { .sequence }
    @usableFromInline var version: OCSP_Version?
    @usableFromInline var responderID: OCSP_ResponderID
    @usableFromInline var producedAt: GeneralizedTime
    @usableFromInline var responses: [OCSP_SingleResponse]
    @usableFromInline var responseExtensions: AuthenticationFramework_Extensions?
    @inlinable init(version: OCSP_Version?, responderID: OCSP_ResponderID, producedAt: GeneralizedTime, responses: [OCSP_SingleResponse], responseExtensions: AuthenticationFramework_Extensions?) {
        self.version = version
        self.responderID = responderID
        self.producedAt = producedAt
        self.responses = responses
        self.responseExtensions = responseExtensions
    }
    @inlinable init(derEncoded root: ASN1Node,
        withIdentifier identifier: ASN1Identifier) throws {
        self = try DER.sequence(root, identifier: identifier) { nodes in
            let version: OCSP_Version = try DER.explicitlyTagged(&nodes, tagNumber: 0, tagClass: .contextSpecific) { node in return try OCSP_Version(derEncoded: node) }
            let responderID: OCSP_ResponderID = try OCSP_ResponderID(derEncoded: &nodes)
            let producedAt: GeneralizedTime = try GeneralizedTime(derEncoded: &nodes)
            let responses: [OCSP_SingleResponse] = try DER.sequence(of: OCSP_SingleResponse.self, identifier: .sequence, nodes: &nodes)
            let responseExtensions: AuthenticationFramework_Extensions? = try DER.optionalExplicitlyTagged(&nodes, tagNumber: 1, tagClass: .contextSpecific) { node in return try AuthenticationFramework_Extensions(derEncoded: node) }
            return OCSP_ResponseData(version: version, responderID: responderID, producedAt: producedAt, responses: responses, responseExtensions: responseExtensions)
        }
    }
    @inlinable func serialize(into coder: inout DER.Serializer,
        withIdentifier identifier: ASN1Identifier) throws {
        try coder.appendConstructedNode(identifier: identifier) { coder in
            if let version = self.version { if let version = self.version { try coder.serialize(explicitlyTaggedWithTagNumber: 0, tagClass: .contextSpecific) { codec in try codec.serialize(version) } } }
            try coder.serialize(responderID)
            try coder.serialize(producedAt)
            try coder.serializeSequenceOf(responses)
            if let responseExtensions = self.responseExtensions { if let responseExtensions = self.responseExtensions { try coder.serialize(explicitlyTaggedWithTagNumber: 1, tagClass: .contextSpecific) { codec in try codec.serialize(responseExtensions) } } }
        }
    }
}
