// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023—2024 Namdak Tönpa.
import SwiftASN1
import Foundation

@usableFromInline indirect enum DSTU_4145Params_definition_Choice: DERImplicitlyTaggable, DERParseable, DERSerializable, Sendable {
    @inlinable static var defaultIdentifier: ASN1Identifier { .enumerated }
        case ecbinary(DSTU_ECBinary)
    case namedCurve(ASN1ObjectIdentifier)
    @inlinable init(derEncoded rootNode: ASN1Node, withIdentifier identifier: ASN1Identifier) throws {
        switch rootNode.identifier {
        case DSTU_ECBinary.defaultIdentifier:
            self = .ecbinary(try DSTU_ECBinary(derEncoded: rootNode, withIdentifier: rootNode.identifier))
        case ASN1ObjectIdentifier.defaultIdentifier:
            self = .namedCurve(try ASN1ObjectIdentifier(derEncoded: rootNode, withIdentifier: rootNode.identifier))
            default: throw ASN1Error.unexpectedFieldType(rootNode.identifier)
        }
    }
    @inlinable func serialize(into coder: inout DER.Serializer, withIdentifier identifier: ASN1Identifier) throws {
        switch self {
        case .ecbinary(let ecbinary):
                        if identifier != Self.defaultIdentifier {
                            try coder.appendConstructedNode(identifier: identifier) { coder in
                                try coder.serialize(ecbinary)
                            }
                        } else {
                            try coder.serialize(ecbinary)
                        }
        case .namedCurve(let namedCurve):
                        if identifier != Self.defaultIdentifier {
                            try coder.appendConstructedNode(identifier: identifier) { coder in
                                try coder.serialize(namedCurve)
                            }
                        } else {
                            try coder.serialize(namedCurve)
                        }

        }
    }

}
