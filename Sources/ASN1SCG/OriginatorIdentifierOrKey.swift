// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023 Namdak Tonpa.
import SwiftASN1
import Crypto
import Foundation

@usableFromInline indirect enum OriginatorIdentifierOrKey: DERParseable, DERSerializable, Hashable, Sendable {
    case issuerAndSerialNumber(IssuerAndSerialNumber)
    case subjectKeyIdentifier(ASN1OctetString)
    case originatorKey(OriginatorPublicKey)
    @inlinable init(derEncoded rootNode: ASN1Node) throws {
        switch rootNode.identifier {
            case IssuerAndSerialNumber.defaultIdentifier:
                self = .issuerAndSerialNumber(try IssuerAndSerialNumber(derEncoded: rootNode))
            case ASN1Identifier(tagWithNumber: 0, tagClass: .contextSpecific):
                self = .subjectKeyIdentifier(try ASN1OctetString(derEncoded: rootNode))
            case ASN1Identifier(tagWithNumber: 1, tagClass: .contextSpecific):
                self = .originatorKey(try OriginatorPublicKey(derEncoded: rootNode))
            default: throw ASN1Error.unexpectedFieldType(rootNode.identifier)
        }
    }
    @inlinable func serialize(into coder: inout DER.Serializer) throws {
        switch self {
            case .issuerAndSerialNumber(let issuerAndSerialNumber): try coder.serialize(issuerAndSerialNumber)
            case .subjectKeyIdentifier(let subjectKeyIdentifier):
                try coder.appendConstructedNode(
                identifier: ASN1Identifier(tagWithNumber: 0, tagClass: .contextSpecific),
                { coder in try coder.serialize(subjectKeyIdentifier) })
            case .originatorKey(let originatorKey):
                try coder.appendConstructedNode(
                identifier: ASN1Identifier(tagWithNumber: 1, tagClass: .contextSpecific),
                { coder in try coder.serialize(originatorKey) })
        }
    }

}
