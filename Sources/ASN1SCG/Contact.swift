// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023 Namdak Tonpa.
import SwiftASN1
import Crypto
import Foundation

@usableFromInline struct Contact: DERImplicitlyTaggable, Hashable, Sendable {
    @inlinable static var defaultIdentifier: ASN1Identifier { .sequence }
    @usableFromInline var nickname: ASN1OctetString
    @usableFromInline var avatar: ASN1OctetString
    @usableFromInline var names: [ASN1OctetString]
    @usableFromInline var phone_id: ASN1OctetString
    @usableFromInline var surnames: [ASN1OctetString]
    @usableFromInline var last_msg: Message
    @usableFromInline var presence: PresenceType
    @usableFromInline var update: ArraySlice<UInt8>
    @usableFromInline var created: ArraySlice<UInt8>
    @usableFromInline var settings: [Feature]
    @usableFromInline var services: [Service]
    @usableFromInline var status: ContactStatus
    @inlinable init(nickname: ASN1OctetString, avatar: ASN1OctetString, names: [ASN1OctetString], phone_id: ASN1OctetString, surnames: [ASN1OctetString], last_msg: Message, presence: PresenceType, update: ArraySlice<UInt8>, created: ArraySlice<UInt8>, settings: [Feature], services: [Service], status: ContactStatus) {
        self.nickname = nickname
        self.avatar = avatar
        self.names = names
        self.phone_id = phone_id
        self.surnames = surnames
        self.last_msg = last_msg
        self.presence = presence
        self.update = update
        self.created = created
        self.settings = settings
        self.services = services
        self.status = status
    }
    @inlinable init(derEncoded root: ASN1Node,
        withIdentifier identifier: ASN1Identifier) throws {
        self = try DER.sequence(root, identifier: identifier) { nodes in
            let nickname = try ASN1OctetString(derEncoded: &nodes)
            let avatar = try ASN1OctetString(derEncoded: &nodes)
            let names = try DER.sequence(of: ASN1OctetString.self, identifier: .sequence, nodes: &nodes)
            let phone_id = try ASN1OctetString(derEncoded: &nodes)
            let surnames = try DER.sequence(of: ASN1OctetString.self, identifier: .sequence, nodes: &nodes)
            let last_msg = try Message(derEncoded: &nodes)
            let presence = try PresenceType(derEncoded: &nodes)
            let update = try ArraySlice<UInt8>(derEncoded: &nodes)
            let created = try ArraySlice<UInt8>(derEncoded: &nodes)
            let settings = try DER.sequence(of: Feature.self, identifier: .sequence, nodes: &nodes)
            let services = try DER.sequence(of: Service.self, identifier: .sequence, nodes: &nodes)
            let status = try ContactStatus(derEncoded: &nodes)
            return Contact(nickname: nickname, avatar: avatar, names: names, phone_id: phone_id, surnames: surnames, last_msg: last_msg, presence: presence, update: update, created: created, settings: settings, services: services, status: status)
        }
    }
    @inlinable func serialize(into coder: inout DER.Serializer,
        withIdentifier identifier: ASN1Identifier) throws {
        try coder.appendConstructedNode(identifier: identifier) { coder in
            try coder.serialize(self.nickname)
            try coder.serialize(self.avatar)
            try coder.serializeSequenceOf(names)
            try coder.serialize(self.phone_id)
            try coder.serializeSequenceOf(surnames)
            try coder.serialize(self.last_msg)
            try coder.serialize(self.presence)
            try coder.serialize(self.update)
            try coder.serialize(self.created)
            try coder.serializeSequenceOf(settings)
            try coder.serializeSequenceOf(services)
            try coder.serialize(self.status)
        }
    }
}
